# 키워드 뉴스 알림 서비스 PRD (Product Requirements Document)

**프로젝트명:** News Ping
**문서 버전:** v1.0
**작성일:** 2026-07-30

---

## 1. 개요 (Overview)

### 1.1 배경
온라인 뉴스는 실시간으로 대량 생산되지만, 사용자가 관심 있는 특정 키워드(인물, 기업, 이슈 등)와 관련된 뉴스를 놓치지 않고 확인하기는 어렵다. 본 서비스는 사용자가 등록한 키워드가 제목 또는 본문에 포함된 뉴스를 실시간으로 감지하여 푸시 알림으로 전달하는 것을 목적으로 한다.

### 1.2 목표
- 여러 온라인 뉴스 사이트를 실시간으로 수집(크롤링)한다.
- 사용자별 등록 키워드와 뉴스 제목/본문을 매칭하여 해당 여부를 판별한다.
- 매칭된 뉴스를 뉴스 링크 및 매칭된 키워드 정보와 함께 즉시 푸시 알림으로 발송한다.
- 다수의 알림이 동시에 도착해도 사용자가 한눈에 확인할 수 있는 UI를 제공한다.
- 애드몹 광고를 사용자 경험을 해치지 않는 선에서 삽입하여 수익화한다.

### 1.3 비목표 (Out of Scope, v1 기준)
- 뉴스 원문 전체 재게시(저작권 이슈로 링크 연결만 제공)
- 키워드 기반 감성 분석/요약 등 AI 부가 기능(추후 확장 검토)
- 무제한 키워드 등록(정책상 10개로 제한)

---

## 2. 사용자 스토리 (User Stories)

| ID | 사용자 스토리 |
|----|----------------|
| US-1 | 사용자로서, 관심 키워드를 최대 10개까지 등록하여 관련 뉴스를 놓치지 않고 받고 싶다. |
| US-2 | 사용자로서, 10개가 모두 채워진 경우 기존 키워드를 수정하거나 삭제한 뒤 새 키워드를 등록하고 싶다. |
| US-3 | 사용자로서, 카카오 또는 네이버 계정으로 간편하게 로그인하고 싶다. |
| US-4 | 사용자로서, 여러 알림이 한꺼번에 와도 리스트에서 한눈에 확인하고 싶다. |
| US-5 | 사용자로서, 알림을 탭하면 어떤 키워드가 매칭되었는지, 그리고 원문 뉴스 링크로 바로 이동하고 싶다. |
| US-6 | 사용자로서, 메인 화면에서 최신 뉴스 흐름을 가볍게 훑어보고 싶다. |
| US-7 | 사용자로서, 마이페이지에서 내 키워드 알림 이력과 키워드 설정을 관리하고 싶다. |
| US-8 | 사용자로서, 개인정보(닉네임, 알림 설정 등)를 직접 수정하고 싶다. 단, 간편로그인으로 가져온 정보(이름, 프로필 등)는 수정할 수 없음을 이해한다. |

---

## 3. 핵심 기능 (Core Features)

### 3.1 뉴스 수집 파이프라인
- **다중 소스 수집:** 주요 온라인 뉴스사(포털 뉴스, 언론사 RSS/API, 필요 시 크롤링)를 대상으로 신규/업데이트 기사를 주기적으로 수집한다.
- **수집 주기:** 뉴스 소스의 특성에 맞춰 짧은 주기(예: 1~5분 간격) 폴링 또는 RSS/웹훅 기반 수집을 병행한다. 완전 실시간(수 초 단위)은 소스 제약상 어려우므로 "준실시간"을 목표로 한다.
- **정규화:** 언론사별로 상이한 구조(제목, 본문, 발행시각, 링크, 언론사명)를 표준 스키마로 정규화하여 저장한다.
- **중복 제거:** 동일 기사가 여러 매체/포털에 중복 노출되는 경우를 식별하여 중복 알림을 방지한다.

### 3.2 키워드 매칭 엔진 (핵심 로직)
- 신규 기사가 수집될 때마다, 전체 사용자의 등록 키워드 목록과 제목/본문을 대조한다.
- **매칭 대상:** 제목 우선 검사 → 본문 검사(성능을 위해 제목에서 조기 판별 가능하면 본문 스캔 생략 가능).
- **매칭 방식(1차):** 단순 포함(substring) 매칭 + 형태소 분석기(예: 한국어 형태소 분석 라이브러리)를 활용한 키워드 정규화(조사 제거, 띄어쓰기 변형 대응)로 정확도를 높인다.
- **매칭 결과:** 기사 1건당 매칭된 키워드 목록과 매칭된 사용자 목록을 생성하여 알림 발송 큐에 적재한다.
- **성능 고려:** 사용자 수 및 키워드 수가 늘어날 경우를 대비해 키워드 인덱싱(예: Trie, Aho-Corasick 알고리즘) 기반 매칭으로 확장 가능하도록 설계한다.

### 3.3 키워드 관리
- 사용자당 키워드 최대 10개 등록.
- 10개 도달 시 신규 등록 버튼 비활성화 + "기존 키워드를 수정 또는 삭제해주세요" 안내 노출.
- 키워드 수정/삭제/재등록 기능 제공.
- 동일 키워드 중복 등록 방지(대소문자/띄어쓰기 정규화 후 비교).

### 3.4 푸시 알림
- 매칭된 뉴스 발생 즉시 푸시 발송.
- **알림 내용 구성:** 매칭된 키워드(하이라이트), 기사 제목, 언론사명, 발행 시각, 원문 링크.
- 다건 동시 발생 시 알림을 그룹화(예: "OO 외 3건의 키워드 뉴스가 도착했습니다")하여 알림 피로도를 낮춘다.
- 알림 탭 시 앱 내 상세 화면 또는 외부 브라우저로 원문 링크 연결.

### 3.5 알림 리스트 (한눈에 보기)
- 마이페이지 내 "키워드 알림 리스트" 화면에서 시간 역순으로 알림 이력을 리스트업.
- 리스트 각 항목에 매칭 키워드 태그, 기사 제목, 언론사, 수신 시각 표시.
- 읽음/안읽음 상태 구분(안읽음 뱃지 카운트).
- 키워드별 필터링/탭 전환 기능(예: 키워드 A만 모아보기).

### 3.6 메인 화면
- 실시간 최신 뉴스 피드(전체 수집 뉴스 중 최신순) 노출.
- 개인화 없이 일반 최신 뉴스 흐름을 가볍게 훑어볼 수 있는 용도.

### 3.7 마이페이지
- 나의 키워드 알림 리스트(3.5)
- 키워드 설정(등록/수정/삭제, 3.3)
- 개인정보 수정(닉네임, 알림 수신 시간대, 알림 On/Off 등)
- 로그인 연동 정보(카카오/네이버)는 조회만 가능, 수정 불가 처리

### 3.8 간편 로그인 (카카오 / 네이버)
- 카카오 로그인, 네이버 로그인 OAuth 연동.
- 최초 로그인 시 제공받는 프로필 정보(이름/닉네임/프로필 이미지 등)는 서비스 내 "개인정보 수정" 화면에서 수정 불가 항목으로 명확히 구분 표시(예: 잠금 아이콘 + "카카오 계정 정보는 카카오에서 관리됩니다" 안내 문구).
- 서비스 자체 설정 항목(알림 설정, 키워드 등)만 앱 내에서 수정 가능.

### 3.9 광고 (AdMob)
- 사용자 경험 저해를 최소화하는 위치/형태로 광고 삽입.
- 예: 메인 뉴스 피드 리스트 중간중간 네이티브 광고, 알림 리스트 화면 하단 배너.
- 전면 광고(인터스티셜) 사용 시 앱 실행 직후나 알림 상세 진입 직후 등 몰입을 방해하는 시점은 지양하고, 화면 전환 빈도가 낮은 지점에 한정.
- 광고 정책은 Google AdMob 정책 및 관련 개인정보 동의(맞춤 광고 동의 등)를 준수.

### 3.10 키워드 트렌드 통계 페이지 (신규)
- **목적:** 애드센스/애드몹 심사에서 요구하는 "서비스 자체의 독창적 콘텐츠"를 확보하기 위한 핵심 페이지. 타 사이트 콘텐츠의 재게시가 아니라, 자체 수집 데이터를 가공한 2차 콘텐츠로 구성.
- **데이터 소스:** 기존 `Article`, `Keyword`, `MatchNotification` 테이블에서 집계 쿼리로 생성(별도 콘텐츠 제작 파이프라인 불필요).
- **제공 콘텐츠(예시):**
  - 최근 24시간 언급량 급상승 키워드 랭킹
  - 키워드별 언급 추이 그래프(일/주 단위)
  - 언론사별 보도량 비교
- **접근 경로:** 로그인 없이도 열람 가능한 공개 페이지로 구성하여 실제 방문 트래픽을 확보(메인 하단 또는 별도 탭에 노출).
- **갱신 주기:** 뉴스 크롤링/매칭 배치와 동일한 주기로 집계 갱신(예: 5~10분 간격, 혹은 통계 성격상 시간 단위로도 무방).

### 3.11 필수 정책 페이지
- **목적:** 콘텐츠 품질과 무관하게 애드센스/애드몹 심사에서 공통적으로 요구되는 최소 요건 충족.
- **페이지 목록:** 서비스 소개, 이용약관, 개인정보처리방침, 문의(연락처)
- **노출 위치:** 마이페이지 최하단에 링크 형태로 고정 노출(모든 로그인 사용자가 접근 가능한 위치에 배치하되, 비로그인 상태에서도 열람 가능하도록 별도 라우팅 제공 권장).
- **작성 기준:** 템플릿 복붙이 아닌 실제 서비스 운영 방식(크롤링 대상, 개인정보 수집 항목, 광고 식별자 처리 등)을 반영한 실질적 내용으로 작성.

### 3.12 최초 로그인 온보딩 가이드
- **목적:** 신규 가입자가 서비스 핵심 사용법(키워드 등록 → 알림 수신 → 알림 리스트 확인)을 빠르게 이해하도록 안내.
- **트리거:** 카카오/네이버 최초 로그인 성공 직후 1회 노출(이후 재노출 안 함, 스킵 가능).
- **구성(3~4단계 슬라이드 또는 모달):**
  1. 서비스 소개(키워드 뉴스 알림이란?)
  2. 키워드 등록 방법 안내(최대 10개 제한 안내 포함)
  3. 알림 리스트 확인 방법 안내
  4. (iOS 사용자 대상) 홈 화면 추가 시 알림 수신 가능 안내
- **구현 방식:** 별도 서버 저장 없이 로컬 온보딩 완료 플래그(사용자 프로필의 `onboarding_completed` 필드) 정도로 최소 구현.

---

## 4. 화면 구성 (Information Architecture)

```
0. 최초 로그인 온보딩 (1회성 모달/슬라이드)
   - 서비스 소개 → 키워드 등록법 → 알림 리스트 확인법 → (iOS) 홈화면 추가 안내

1. 로그인 화면
   - 카카오 로그인 / 네이버 로그인

2. 메인 (홈)
   - 최신 뉴스 피드
   - 키워드 트렌드 통계 (급상승 랭킹 / 언급 추이 / 언론사별 보도량) — 비로그인도 열람 가능
   - 광고 (네이티브, 피드/통계 페이지 중간 삽입)

3. 키워드 알림 리스트 (마이페이지 하위)
   - 알림 이력 리스트 (키워드 태그 / 제목 / 언론사 / 시각)
   - 읽음/안읽음 표시
   - 키워드별 필터

4. 마이페이지
   - 나의 키워드 알림 (3번 화면 진입)
   - 키워드 설정 (등록/수정/삭제, 최대 10개)
   - 개인정보 수정 (수정 가능 항목 / 수정 불가 항목 구분)
   - 로그아웃
   - [하단 고정] 서비스 소개 / 이용약관 / 개인정보처리방침 / 문의
```

---

## 5. 데이터 모델 (초안)

| 엔티티 | 주요 속성 |
|--------|-----------|
| User | user_id, provider(kakao/naver), provider_uid, nickname(수정가능), profile_image(수정불가, provider 원본), 알림설정, 생성일 |
| Keyword | keyword_id, user_id, keyword_text, 정규화된 keyword_text, 등록일 |
| Article | article_id, source(언론사), title, body, url, published_at, 수집일 |
| MatchNotification | notification_id, user_id, article_id, matched_keywords[], 발송상태, 읽음여부, 발송시각 |

---

## 6. 비기능 요구사항 (Non-Functional Requirements)

- **실시간성:** 기사 발행 후 알림 도착까지의 지연을 최소화(목표 수 분 이내).
- **확장성:** 사용자 수 및 키워드 수 증가에 따른 매칭 연산 부하를 고려한 인덱싱/큐잉 구조 필요.
- **정확도:** 오탐(false positive) 최소화를 위한 키워드 매칭 로직 고도화(형태소 분석, 동음이의어 처리 등은 향후 개선 과제).
- **개인정보 보호:** 관련 법령(개인정보보호법 등) 준수. 위치 정보 등 민감 정보 수집 시 반드시 선택적 동의로 설계하고, 서비스 이용에 필수가 아닌 개인정보를 필수 동의 항목으로 강제하지 않는다.
- **알림 스팸 방지:** 동일 사용자에게 짧은 시간 내 다수 알림이 몰릴 경우 그룹화하여 발송 빈도를 제어.
- **가용성:** 뉴스 수집/매칭/푸시 파이프라인 중 일부 장애 시에도 알림 유실을 최소화하는 재시도/큐 구조 확보.

---

## 7. 리스크 및 고려사항

| 항목 | 내용 |
|------|------|
| 저작권 | 뉴스 본문 전체 노출은 저작권 침해 소지가 있어 제목/요약 일부 + 원문 링크로만 제공 |
| 크롤링 정책 | 언론사/포털별 크롤링 약관 및 robots.txt 준수 여부 확인 필요, 가능한 경우 공식 API/RSS 우선 사용 |
| 알림 지연 | 사용하는 푸시 서비스(FCM 등)의 큐잉 정책상 최대 지연이 발생할 수 있어, 오래된 알림은 발송 생략 또는 TTL 설정 필요 |
| 개인정보 | 간편로그인 제공 정보와 서비스 자체 정보의 수정 권한 분리를 UX상 명확히 구분해야 함 |
| 광고 UX | 광고 삽입 위치/빈도가 과도할 경우 이탈률 상승 우려 — 알림 확인 플로우를 방해하지 않는 위치 선정 필요 |

---

## 8. 애드센스/애드몹 심사 대응 검토

위 3.10~3.12 반영 시 구조적으로는 애드센스 최소 요건(독창적 콘텐츠, 명확한 탐색 구조, 정책 페이지)을 충족하지만, **아래 항목은 구조 개선과 별개로 남는 리스크**이므로 반드시 함께 관리해야 한다.

| 항목 | 충족 여부 | 비고 |
|------|-----------|------|
| 독창적 콘텐츠 | ✅ (3.10 트렌드 통계로 충족) | 자체 집계 데이터 기반이라 복제 콘텐츠 리스크 낮음 |
| 명확한 탐색 구조 / 정책 페이지 | ✅ (3.11 반영) | 템플릿 형태가 아닌 실제 내용 기입 필수 |
| 실제 트래픽 | ⚠️ 별도 확보 필요 | 콘텐츠 구조가 갖춰져도 방문자가 없으면 심사·매출 모두 의미 없음. 출시 후 일정 기간 실사용자 트래픽을 쌓은 뒤 신청 권장 |
| 콘텐츠 지속성 | ⚠️ 운영 이슈 | 트렌드 통계는 자동 갱신되어 유지되지만, 정책 페이지 등은 서비스 변경 시 최신화 필요 |
| 개인정보/동의 관리 | ⚠️ 별도 구현 필요 | 맞춤광고 동의(CMP), 개인정보처리방침 내 광고 식별자 수집 고지 등 최신 정책 반영 필요 |

**결론:** 3.10~3.12를 반영하면 "콘텐츠 구조" 측면의 심사 리스크는 상당히 낮아지지만, 승인을 보장하지는 않는다. Google 심사는 콘텐츠 구조 외에 실제 트래픽과 사용자 가치를 함께 평가하므로, 서비스를 먼저 정식 오픈해 일정 기간(수 주) 실사용 데이터를 쌓은 뒤 신청하는 것을 권장한다.

---

## 9. 향후 확장 아이디어 (참고용, v1 범위 아님)

- 키워드 등록 개수 유료 확장(구독 모델)
- 키워드 관련 뉴스 요약/감성 분석 제공
- 키워드 트렌드(언급량 추이) 시각화

---

## 10. 개발 환경 및 운영 노트 (Dev/Ops Notes)

> 실제 개발/디버깅 과정에서 확인된 사실들. PRD 자체는 아니지만 다음 세션이 바로 이어서 작업할 수 있도록 반드시 참고할 것. 특정 시점의 작업 로그는 `docs/devlog-2026-07-31.md` 참고.

### 10.1 로컬 개발 서버
- API: `npm run dev:api` (apps/api, 포트 4000, Express + tsx watch)
- Web: `npm run dev:web` (apps/web, 포트 3000 기본이나 다른 프로세스가 점유하면 3001로 밀림 — LAN/모바일 테스트 시엔 카카오 리다이렉트 URI와 맞춰야 하므로 `cd apps/web && npx next dev -p 3001`로 포트를 명시 고정할 것)
- `.env`(apps/api) / `.env.local`(apps/web)은 **프로세스 시작 시점에만 로드**된다(dotenv). 수정 후에는 반드시 서버를 완전히 재시작해야 반영된다 (hot reload 안 됨).
- tsx watch가 크래시 후 포트를 계속 물고 있는 경우가 잦다. 재시작 전에 항상:
  ```
  pkill -9 -f "tsx watch src/server.ts"
  lsof -iTCP -sTCP:LISTEN -P -n | grep :4000 | awk '{print $2}' | xargs -r kill -9
  ```

### 10.2 모바일/LAN 테스트
- 맥의 로컬 IP는 `ipconfig getifaddr en0`로 확인 (⚠️ `curl ifconfig.me`는 공인 IP라 LAN 테스트에 쓸 수 없음). 이 프로젝트 개발 환경 기준 `192.168.35.65`.
- `apps/web/next.config.ts`에 `allowedDevOrigins: ["192.168.35.65"]` 추가되어 있음 — 다른 네트워크/IP로 바뀌면 이 값도 갱신 필요.
- **iOS Safari는 HTTPS(또는 정확히 `localhost`)가 아니면 Service Worker/Push API 자체가 생성되지 않는다.** `http://<LAN IP>:3001`처럼 평범한 HTTP LAN 주소는 iOS 기준 보안 컨텍스트가 아니어서 웹 푸시를 절대 테스트할 수 없다 (iOS 버전, 홈 화면 추가 여부와 무관). 모바일에서 푸시까지 테스트하려면 HTTPS 터널(ngrok/Cloudflare Tunnel 등) 또는 실제 배포 도메인이 필요 — **2026-07-31 기준 미해결.**
- 카카오 로그인(아래 10.3)까지 테스트하려면 웹/API의 `KAKAO_REDIRECT_URI` 값을 현재 접속 방식(localhost vs LAN IP vs 프로덕션 도메인)에 맞는 값으로 맞춰서 재시작해야 함 — 세 값을 동시에 쓸 수 없고 하나만 활성화됨.

### 10.3 카카오 로그인
- 카카오 디벨로퍼스 앱 "뉴스핑" (app id 1529370)에 리다이렉트 URI 3개 등록됨:
  - `https://saerolab.com/login/kakao/callback` (프로덕션)
  - `http://localhost:3001/login/kakao/callback` (데스크톱 로컬)
  - `http://192.168.35.65:3001/login/kakao/callback` (LAN/모바일 로컬 — IP 바뀌면 재등록 필요)
- **리다이렉트 URI는 반드시 `/login/kakao/callback` (Next.js 페이지, code를 파싱해 API를 POST로 호출)이어야 하며, `/api/auth/kakao/callback` (백엔드 POST 전용 엔드포인트)를 직접 가리키면 안 된다.** 브라우저 리다이렉트가 그 페이지로 갈 수 없기 때문.
- `KAKAO_CLIENT_ID`는 반드시 **REST API 키**여야 함(JS/Native/Admin 키 아님).
- Client Secret이 카카오 콘솔에서 활성화되어 있어 토큰 교환 시 `KAKAO_CLIENT_SECRET`이 필수 (`apps/api/src/api/auth/providers/kakao.ts`).
- 웹의 `NEXT_PUBLIC_KAKAO_REDIRECT_URI`와 API의 `KAKAO_REDIRECT_URI`는 항상 **완전히 동일한 값**이어야 함.

### 10.4 Firebase
- 프로젝트: `news-ping-38eda`. 두 가지 설정을 절대 혼동하지 말 것:
  - **클라이언트 웹 설정**(apiKey/authDomain/projectId/storageBucket/messagingSenderId/appId) → `apps/web/.env.local`의 `NEXT_PUBLIC_FIREBASE_*` (비밀값 아님, 브라우저에 노출되어도 무방)
  - **Admin SDK 서비스 계정 JSON**(type/project_id/private_key/client_email 등, Firebase 콘솔 > 프로젝트 설정 > 서비스 계정 > 새 비공개 키 생성) → `apps/api/.env`의 `FIREBASE_SERVICE_ACCOUNT_JSON`
- `FIREBASE_SERVICE_ACCOUNT_JSON`은 **반드시 한 줄**이어야 한다. dotenv는 멀티라인 값을 지원하지 않아 pretty-print된 JSON을 그대로 붙여넣으면 `{` 한 글자만 읽혀 서버가 크래시한다.
- VAPID 키(웹 푸시 인증서, Firebase 콘솔 > 프로젝트 설정 > Cloud Messaging 탭) → `apps/web/.env.local`의 `NEXT_PUBLIC_FIREBASE_VAPID_KEY`.
- 클라이언트 푸시 코드: `apps/web/lib/firebase.ts`, `apps/web/lib/push.ts`, `apps/web/public/firebase-messaging-sw.js`, 등록 UI는 `apps/web/app/mypage/profile/page.tsx`. `firebase/messaging`의 `isSupported()`는 iOS Safari를 오탐(false)하는 경우가 많아 사용하지 않고 `Notification`/`serviceWorker`/`PushManager` 존재 여부를 직접 확인한다. iOS non-standalone 체크가 이 존재 여부 체크보다 **먼저** 와야 정확한 안내 문구가 나온다 (순서 중요).
- `apps/api/src/db/repositories/push-token.repository.ts` 기준, 2026-07-31 세션 종료 시점 등록된 푸시 토큰 0개 — 아직 실기기 푸시 수신은 검증 안 됨 (10.2의 HTTPS 이슈 때문).

### 10.5 뉴스 크롤링
- `apps/api/src/pipeline/crawler/sources.ts`에 12개 소스 등록: 연합뉴스, 동아일보, 경향신문, 한겨레, 세계일보, 국민일보, 한국경제, 서울경제, 머니투데이, 이데일리, 아시아경제, 구글뉴스(한국).
- 제외됨(RSS 서비스 종료/차단 확인): 조선일보, 중앙일보, 문화일보, 서울신문, 파이낸셜뉴스, 한국일보, 매일경제(Cloudflare 차단), 네이버뉴스·다음뉴스(포털 통합 RSS 미제공).
- `apps/api/src/pipeline/crawler/index.ts`: 언론사마다 봇 차단 기준이 반대라(일부는 User-Agent 없어야 통과, 일부는 있어야 통과) User-Agent 없이 먼저 요청 후 실패 시에만 붙여서 재시도한다. 국민일보는 EUC-KR 응답이라 XML 프롤로그의 `encoding`을 감지해 `iconv-lite`로 변환한다.
- 수동 트리거: `curl -H "Authorization: Bearer <CRON_SECRET>" http://localhost:4000/internal/pipeline/crawl-and-match`
- 키워드 매칭은 **그 크롤 실행에서 새로 수집된 기사에만** 적용된다 (이미 저장된 기사에 소급 적용 안 됨).

### 10.6 DB
- Neon Postgres 연결됨, 초기 마이그레이션 `prisma/migrations/20260730162258_init` 적용 완료. 스키마 변경 시 `cd apps/api && npx prisma migrate dev --name <설명>`.

### 10.7 보안 — 미해결 TODO
- **`apps/api/.env.example`(git 추적 파일)에 실제 Neon DB 비밀번호와 카카오 Client Secret이 그대로 남아있다.** 커밋 전 반드시 빈 플레이스홀더로 정리할 것. `.env`/`.env.local`은 정상적으로 gitignore 처리되어 있어 안전함.

### 10.8 배포 (아직 미실행, 코드 기준 확인된 내용만)
- `apps/web/vercel.ts`: `/api/*` → `${API_ORIGIN}/api/$1` rewrite (web Vercel 프로젝트에 `API_ORIGIN` env 필요, 예: `https://newkey-api.vercel.app`)
- `apps/api/vercel.ts`: 전체 요청을 `/api/$1`로 rewrite + `crawl-and-match`를 5분마다 도는 Vercel Cron 등록(해당 프로젝트에 `CRON_SECRET` env 설정 시 자동으로 Authorization 헤더 실어 보냄)
- 프로덕션에선 `KAKAO_REDIRECT_URI`/`NEXT_PUBLIC_KAKAO_REDIRECT_URI`를 `https://saerolab.com/login/kakao/callback`으로, `DATABASE_URL`(pooled)과 `DIRECT_URL`(unpooled)을 분리 설정, `FIREBASE_SERVICE_ACCOUNT_JSON`도 한 줄로, `WEB_ORIGIN`도 실제 프로덕션 도메인으로 설정해야 함.

---

*본 문서는 초기 요구사항 정의를 위한 v1 초안이며, 상세 API 명세 및 화면 설계(와이어프레임)는 후속 문서에서 다룬다.*
